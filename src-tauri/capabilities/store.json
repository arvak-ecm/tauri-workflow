{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "store",
  "local": true,
  "description": "Capability for the main store",
  "windows": ["main"],
  "permissions": ["store:allow-set", "store:default"],
  "platforms": ["macOS", "windows"]
}
